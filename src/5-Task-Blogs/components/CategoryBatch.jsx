const categoryBatch = ({name}) => {
    return (
      <div className="px-3 py-1 rounded-full bg-yellow-brand text-sm text-black/80 w-fit">
        {name}
      </div>
    );
}

export default categoryBatch;